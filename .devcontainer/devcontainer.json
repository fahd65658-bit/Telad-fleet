{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "postCreateCommand": "npm ci && cd backend && npm ci && if [ ! -f .env ]; then cp .env.example .env; fi",
  "remoteEnv": {
    "NODE_ENV": "development"
  }
}
