name: Vercel production disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Explain production backend
    runs-on: ubuntu-latest

    steps:
      - name: Vercel deploys are disabled
        run: |
          echo "Production write traffic must not go to api/index.js on Vercel."
          echo "Use deployment/deploy.sh or deployment/docker-compose.yml with root server.js and persistent DATA_DIR."
